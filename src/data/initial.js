import { Map, List } from "immutable";

const initial = Map({
	players: List(["Adam", "Betty", "Cindy", "Dennis"]),
});

export default initial;
// , "Elton", "Adam", "Betty", "Cindy", "Dennis", "Elton"